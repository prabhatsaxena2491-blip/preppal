services:
  - type: web
    name: preppal
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: PREPPAL_URL
        value: https://preppal-ai.com
